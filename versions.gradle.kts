extra.apply {
    // gradle plugin dependencies
    set("gradle_credentials_plugin_version", "1.0.7")
    set("dokka_gradle_plugin_version", "0.9.18")
    set("gradle_node_plugin_version", "1.5.3")
    set("gradle_nexus_plugin_version", "2.3.1")
    set("gradle_nexus_staging_plugin_version", "0.21.0")
    set("kotlin_frontend_plugin_version", "0.0.45")

    // general dependencies
    set("kotlin_version", "1.3.50-eap-54")

    // kotlin-atlaskit
    set("react_version", "16.8.6")
}
